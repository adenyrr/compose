# Step-CA Configuration

# Nom de l'autorité de certification
STEP_CA_NAME=My Internal CA

# DNS/Hostname du serveur CA
STEP_CA_DNS=acme.domain.internal

# Nom du provisioner ACME
STEP_PROVISIONER_NAME=acme

# Email de l'administrateur (pour le provisioner JWK)
STEP_ADMIN_EMAIL=sec@domain.tld

# Port d'exposition (par défaut: 9000)
STEP_CA_PORT=9000

# Mot de passe du CA (sera généré au premier démarrage si vide)
# IMPORTANT: Récupérez ce mot de passe après le premier lancement avec:
# docker logs step-ca 2>&1 | grep "Password:"
STEP_CA_PASSWORD=