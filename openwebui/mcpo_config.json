{
  "mcpServers": {

    "filesystem": {
      "_comment": "Lecture/écriture de fichiers dans /data. Package npm officiel Anthropic. Permet de lire des CSV, TXT, JSON uploadés par les utilisateurs.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/data"]
    },

    "sqlite": {
      "_comment": "Base de données relationnelle persistante. Parfait pour le suivi longitudinal : scores fonctionnels kiné, données de cohorte psychologie, performances sportives. Package PyPI officiel Anthropic.",
      "command": "uvx",
      "args": ["mcp-server-sqlite", "--db-path", "/data/sqlite/platform.db"]
    },

    "python": {
      "command": "uvx",
      "args": ["mcp-server-python"]
    },

    "duckdb": {
      "_comment": "Base de données OLAP ultra-rapide. Parfait pour l'analyse de gros fichiers CSV/Parquet (ex: données brutes de capteurs sportifs, logs d'accéléromètres) que SQLite peine à traiter.",
      "command": "uvx",
      "args": ["mcp-server-duckdb", "--db-path", "/data/duckdb/research_analytics.db"]
    },

    "git": {
      "command": "uvx",
      "args": ["mcp-server-git"]
    },

    "memory": {
      "_comment": "Graphe de connaissances JSON persistant entre sessions. Chaque utilisateur peut y stocker ses concepts clés, protocoles favoris, hypothèses de recherche. Survit aux redémarrages du conteneur.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "env": {
        "MEMORY_FILE_PATH": "/data/memory/knowledge_graph.json"
      }
    },

    "sequential-thinking": {
      "_comment": "Force la décomposition des problèmes complexes en étapes avant de répondre. Améliore fortement la qualité des raisonnements cliniques, diagnostics différentiels, et plans de recherche.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    },

    "wolfram-alpha": {
      "_comment": "Moteur de connaissances computationnel. Indispensable pour valider des calculs de biomécanique, des formules chimiques ou des statistiques sans erreur de calcul du LLM. Nécessite un AppID gratuit.",
      "command": "uvx",
      "args": ["mcp-server-wolfram-alpha"],
      "env": {
        "WOLFRAM_ALPHA_APP_ID": "TON_APP_ID"
      }
    },

    "pubmed-search": {
      "_comment": "Le serveur le plus complet : agrège PubMed, Europe PMC, Semantic Scholar et OpenAlex. Gère les citations et les essais cliniques.",
      "command": "uvx",
      "args": ["pubmed-search-mcp"],
      "env": {
        "NCBI_EMAIL": "ton-email@institution.be",
        "NCBI_API_KEY": "TA_CLE_API_NCBI",
        "SEMANTIC_SCHOLAR_API_KEY": "OPTIONNEL_SI_TU_EN_AS_UNE"
      }
    },

    "arxiv": {
      "_comment": "Recherche et téléchargement de preprints ArXiv. Neurosciences, biomécanique, psychologie cognitive, sciences du sport de haut niveau. Accès avant publication peer-reviewed. Stocke les PDFs localement pour analyse offline.",
      "command": "uvx",
      "args": ["arxiv-mcp-server", "--storage-path", "/data/arxiv"]
    },

    "wikipedia": {
      "_comment": "Recherche Wikipedia",
      "command": "uvx",
      "args": ["wikipedia-mcp"]
    },

    "youtube-transcript": {
      "_comment": "Transcription de vidéos YouTube avec sélection de langue. Utile pour analyser des cours magistraux, conférences TED médicales, démonstrations de techniques gestuelles sportives ou de rééducation.",
      "command": "uvx",
      "args": ["mcp-youtube-transcript"]
    }

  }
}